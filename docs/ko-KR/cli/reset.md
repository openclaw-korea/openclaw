---
summary: "`openclaw reset` CLI 참조 (로컬 상태/설정 초기화)"
read_when:
  - CLI 설치를 유지하면서 로컬 상태를 초기화하고 싶을 때
  - 제거될 항목의 드라이 런을 보고 싶을 때
title: "reset"
---

# `openclaw reset`

로컬 설정/상태를 초기화합니다 (CLI는 설치된 상태로 유지).

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
