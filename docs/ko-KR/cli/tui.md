---
summary: "`openclaw tui` CLI 참조 (게이트웨이에 연결된 터미널 UI)"
read_when:
  - 게이트웨이용 터미널 UI를 사용하고 싶을 때 (원격 접속 친화적)
  - 스크립트에서 url/token/session을 전달하고 싶을 때
title: "tui"
---

# `openclaw tui`

게이트웨이에 연결된 터미널 UI를 엽니다.

관련 문서:

- TUI 가이드: [TUI](/tui)

## 예제

```bash
openclaw tui
openclaw tui --url ws://127.0.0.1:18789 --token <token>
openclaw tui --session main --deliver
```
